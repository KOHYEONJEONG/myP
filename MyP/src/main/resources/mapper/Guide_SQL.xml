<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="guide">

<select id="guideList" parameterType="hashmap" resultType="hashmap">
	SELECT TOP_CHATBOT_NUM, GUIDE_NUM, MENU, STEP, ANSWER_CON
	FROM GUIDE
	WHERE DEL = 1
	<if test="top_num == null and top_num == ''">
	AND TOP_CHATBOT_NUM IS NULL
	</if> 
	<if test="top_num != null and top_num != ''">
	AND TOP_CHATBOT_NUM  = #{top_num}
	</if>
	<!-- 페이징 필요 없음 -->
</select>

<select id="guideCnt" parameterType="hashmap" resultType="Integer">
	SELECT COUNT(*) AS CNT
	FROM GUIDE
	WHERE DEL = 1
	AND TOP_CHATBOT_NUM = #{top_num}
</select>


</mapper>